apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudkitchen-config
  labels:
    {{- include "cloudkitchen.labels" . | nindent 4 }}
data:
  AWS_REGION: {{ .Values.global.region | quote }}
  HF_MODEL: {{ .Values.config.hfModel | quote }}
